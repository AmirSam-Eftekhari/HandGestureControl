# Core
numpy>=1.26,<3.0
scipy>=1.11

# Computer vision / ML
opencv-python-headless>=4.9
mediapipe>=0.10.14

# UI
PySide6>=6.6

# Testing
pytest>=8.0

# --- Optional, platform-dependent extras ---
# Keyboard-shortcut / media-key actions (Settings > Gesture Mapping).
# The app runs fully without this -- app/actions/system_actions.py
# detects its absence and disables just those specific actions.
pyautogui>=0.9.54

# Windows-only system volume control backend. Harmless to omit on
# macOS/Linux, which use the OS's own command-line tools instead.
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2; sys_platform == "win32"
